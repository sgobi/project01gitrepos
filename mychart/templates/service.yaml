apiVersion: v1
kind: Service
metadata:
  name: "myapp-service"
spec:
  selector:
    app: "myapp"
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30002
  type: NodePort
